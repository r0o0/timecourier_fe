import { style } from '@vanilla-extract/css';

export const letterBoxCardWrapperStyle = style({
  position: 'relative',
});

export const letterBoxCardTrashIconStyle = style({
  position: 'absolute',
  top: 8,
  right: 8,
  width: 24,
  height: 24,
});
